# 学习进度打卡

> **用法**：每完成一项，就把 `- [ ]` 改成 `- [x]`，并在文末「进度记录」表追加一行。
> **图例**：
> - 📘 阅读理解　｜　🛠️ 动手运行 / 实验　｜　✍️ 自己写代码（最小版里没有，需你实现）
> - 🔍 读 / 跑最小版 harness（代码已提供，但**必须亲手跑通**才算完成）
>
> 核心参考：[`README.md`](README.md)（概念）＋ [`harness/harness.ts`](harness/harness.ts)（最小版代码）＋ `../deepseek-harness`（你的 monorepo）。

---

## 阶段 0：环境准备

- [x] Node.js 版本确认（v22.19.0，可跑 `.ts`）
- [x] 最小版 harness 就位（`harness/harness.ts`，语法检查通过）
- [ ] 🔍 复制 `harness/.env.example` 为 `harness/.env` 并填入 DEEPSEEK_API_KEY
- [ ] 🔍 跑通首个任务：`cd harness && node --experimental-strip-types harness.ts "在工作区写一个 hello.js，用 node 运行，输出 Hello Agent"`
- [ ] 📘 看懂 `harness/workspace/` 和 `harness/state.json` 分别在任务中扮演什么角色

---

## 阶段 1：概念框架（读 README 第 2~6 节）

- [ ] 📘 能画出数据流：`messages → LLM → {thought, action, done} → 执行 → 回填`
- [ ] 📘 能区分 **harness（确定性代码）** 与 **agent（模型行为）**
- [ ] 📘 能说出「自进化」三个层面，并指出哪个不是模型训练
- [ ] 📘 能说出 `reasoning_content` 是什么、为什么要捕获

---

## 阶段 2：API + 协议（实践 1、2）

- [ ] 📘 读通 `harness.ts` 的 `chat()`
- [ ] 🛠️ 跑一次对话，观察 `🤔` 的思考与最终回答的区别
- [ ] 🛠️ 把 `MODEL` 改成 `deepseek-chat` 再跑一次，对比差异
- [ ] 📘 读通 `TOOLS` 与 `SYSTEM`（为什么用 JSON 协议而不是官方 function calling）
- [ ] 🛠️ 让模型返回一个 `{action, done}` JSON（不接执行器，只看协议）
- [ ] ✍️ 加一个「换模型」开关（env 变量驱动，参考 `.env.example` 注释）

---

## 阶段 3：闭环（实践 3、4）

- [ ] 📘 读通 `executeTool()`，说出「失败返回而不是抛异常」和「输出截断」两点的原因
- [ ] 🛠️ 跑通「写代码 → 编译/运行 → 报错 → 修复」完整闭环
- [ ] 🛠️ 在 `parseJson` 里故意 throw，观察「错误回喂重试」而不是退出
- [ ] ✍️ 给 `executeTool` 加一个新工具（如 `grep` 或 `copy_file`）

---

## 阶段 4：状态与上下文（实践 5）

- [ ] 📘 读通 `saveState` / `loadState`，讲清「内存当外存用」
- [ ] 🛠️ 长任务跑一半 Ctrl+C，重启验证断点续跑（删 `state.json` 可重来）
- [ ] 📘 读通 Token 预算递增（`maxTokens`），说清为什么先小后大
- [ ] ✍️ 实验不同 `GROWTH` 数值对长任务输出的影响
- [ ] ✍️（进阶）加上下文剪枝：消息超过阈值时把旧消息摘要后丢弃

---

## 阶段 5：自进化（实践 6）

- [ ] ✍️ 加 `write_learning` 工具，把任务结论追加进 `LEARNINGS.md`
- [ ] 🛠️ 下次启动自动加载 `LEARNINGS.md` 注入 `SYSTEM`，验证跨会话变稳
- [ ] ✍️（挑战）把工具清单改成数据驱动（`tools.json`）
- [ ] ✍️（挑战）让模型给自己加一个 `grep` 工具，下一轮立即生效

---

## 阶段 6：对照 monorepo（`../deepseek-harness`）

- [ ] 📘 通读 `../deepseek-harness/README.md`，了解它是什么形态的 monorepo
- [ ] 📘 在 `packages/core/agent-loop/` 找到主循环，与最小版 `main()` 对照
- [ ] 📘 在 `packages/core/tools/` + `packages/shell/` 找到工具执行，与 `executeTool()` 对照
- [ ] 📘 在 `packages/llm/llm-deepseek/` 找到 DeepSeek 客户端，与 `chat()` 对照
- [ ] 📘 在 `packages/spill/` 读「上下文外溢」，与最小版「状态落盘」对照（本阶段重点）
- [ ] 📘 在 `packages/session/`、`packages/storage/` 找到会话/存储，与 `state.json` 对照
- [ ] ✍️ 填完映射表后，能给 monorepo 讲「这一包对应最小版哪一段」

---

## 阶段 7：进阶方向

- [ ] ✍️ `stream: true` 实时流式输出
- [ ] ✍️ 换 Claude 的 tool use 客户端，协议不变
- [ ] ✍️ 加任务清单（ToDo）工具、并行工具、错误重试策略
- [ ] 📘 读原版 `steipete/deepseek-harness` 单文件源码

---

## 进度记录

| 日期 | 完成内容 | 备注 |
|---|---|---|
| 2026-08-15 | 阶段 0：环境准备（node 确认、最小 harness 就位） | 学习专区搭建完成 |
| | | |
| | | |
| | | |
